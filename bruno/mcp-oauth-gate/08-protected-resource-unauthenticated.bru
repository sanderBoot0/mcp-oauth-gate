meta {
  name: protected resource - unauthenticated request is rejected (via auth_request)
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/mcp
  body: none
}

assert {
  res.status: eq 401
  res.headers['www-authenticate']: contains resource_metadata
}
